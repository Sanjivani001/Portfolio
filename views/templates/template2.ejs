<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= portfolio.name %>'s Portfolio</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: #121212;
      color: #f5f5f5;
    }
    .container {
      max-width: 1000px;
      margin: auto;
      padding: 30px;
    }
    h1, h2 {
      color: #00bcd4;
    }
    a {
      color: #4dd0e1;
      text-decoration: none;
    }
    .section {
      margin-bottom: 40px;
    }
    .project {
      margin-bottom: 25px;
      border-bottom: 1px solid #333;
      padding-bottom: 15px;
    }
    .project img {
      max-width: 300px;
      border: 2px solid #555;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= portfolio.name %></h1>
    <h2><%= portfolio.title %></h2>
    <div class="section"><strong>About:</strong><br><%= portfolio.about %></div>
    <div class="section"><strong>Skills:</strong> <%= portfolio.skills %></div>
    <div class="section">
      <p><strong>Email:</strong> <%= portfolio.email %></p>
      <p><strong>Phone:</strong> <%= portfolio.phone %></p>
      <p><strong>GitHub:</strong> <a href="<%= portfolio.github %>"><%= portfolio.github %></a></p>
      <p><strong>LinkedIn:</strong> <a href="<%= portfolio.linkedin %>"><%= portfolio.linkedin %></a></p>
    </div>

    <div class="section">
      <h2>Projects</h2>
      <% portfolio.projects.forEach(project => { %>
        <div class="project">
          <h3><%= project.title %></h3>
          <p><%= project.description %></p>
          <p><a href="<%= project.link %>" target="_blank">Project Link</a></p>
          <% if (project.image) { %>
            <img src="<%= project.image %>" alt="Project Image">
          <% } %>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>
